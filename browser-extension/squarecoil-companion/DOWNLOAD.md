# Download

The packaged release branch for this extension is `release/squarecoil-companion`.

Use the branch ZIP for a clean extension-only download. The branch is intended for installation packages only; development remains on `main` under `browser-extension/squarecoil-companion`.

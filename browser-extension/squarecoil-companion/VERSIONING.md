# Versioning

SquareCoil Companion uses three-part extension versions (`MAJOR.MINOR.PATCH`). The installed version is the `version` field in `manifest.json` and is surfaced directly in the popup through `chrome.runtime.getManifest().version`.

# SquareCoil Companion Rebuild
## B5-C Probe-Backed Theme Delta

**Status:** B5-C accepted at exact source `aeac7fe52a2e723106340bc6d283d2eb49521573`; separately authorized B5-D accepted at exact implementation source `4686ab0c92ce7e4b91a92bc64b479a2bd0ea01c0`. See `implementation/B5C-PROBE-THEME-EVIDENCE.md` and `implementation/B5D-THEME-UI-EVIDENCE.md`.
**Source evidence:** `codex/squarecoil-theme-v2.3.3` at `0e6e6ef36534b33383358b4223ae1ae9054848aa`
**Production dependency:** None. The Tampermonkey userscript is evidence only.
**Purpose:** Port a small set of read-only live-probe findings into the extension-owned Sleek Dark theme without reopening broad site-reskin ownership.

## Current B5-C light slice

The extension may add only these probe-confirmed presentation repairs:

1. persistent top-right user/help dropdown surfaces;
2. exact `/leads.php` AdminDesigns filter controls;
3. exact `/calendar.php` FullCalendar day, event, and progress-strip surfaces.

The implementation remains CSS-only after bounded pathname classification. It adds no permission, storage, network request, MutationObserver, timer, or SquareCoil business action. FullCalendar event border colors remain native semantic schedule data.

Original and forced-colors fallback must remove both the owned style layer and the owned route marker. Sleek Dark failure remains presentation-local and cannot degrade Timer authority.

## B5-C acceptance

Before this slice is accepted:

- route classification must be exact and default to `GENERIC`;
- repeated application must retain one owned style layer;
- Original and teardown must remove route/theme ownership;
- native FullCalendar event border colors must not be replaced;
- earlier aggregate, prototype, packaging, and no-native-mutation gates must remain green;
- installed Chrome and Edge must verify both top-right menus, Leads filters, and Install Calendar with Sleek Dark on and native restoration after Original.

## Accepted B5-D probe parity

The separately authorized B5-D milestone completed:

- CKEditor iframe-document theming and toolbar icon contrast;
- DataTables, Select2, QTip, FancyBox, Magnific Popup, Dropzone, and Gantt adapters;
- remaining audited route families and narrow-viewport/print reconciliation;
- complete enable, disable, teardown, accessibility, and installed-browser evidence across those surfaces.

B5-D remains presentation-only, extension-owned, route-bounded, and separately accepted. It does not import or execute the Tampermonkey runtime wholesale.

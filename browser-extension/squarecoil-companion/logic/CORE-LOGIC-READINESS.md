# SquareCoil Companion Rebuild
## Core Logic Readiness Checkpoint

**Status:** CORE BEHAVIOR IMPLEMENTATION-READY  
**Active branch:** `proto/squirel-coil-plugin`  
**Role boundary:** Logic Systems Architect  
**Purpose:** preserve the current behavior-layer checkpoint so future work does not reopen settled Logic without a real contradiction.

---

# 1. Core Result

The planned core rebuild now has explicit behavior sufficient for downstream implementation without inventing core product logic.

Settled authority includes:

```text
L0  Invariants
L1  Lifecycle / READY / recovery
L2  Timer State / Ledger / migration
L3  SquareCoil read-only Bridge
L4  Timer transitions / failure / recovery
L5  Time views / workspace
L5A Live tab Today / thresholds / native focus intent
L6  Archive / data safety / backup / CSV
L7  Settings / themes / limits / support
L8  acceptance behavior

B2-C completion behavior
B3 Workspace readiness audit
B4 Data readiness audit
B5 Core Settings/Presentation readiness audit
```

**No core behavior-layer blocker remains.**

---

# 2. Settled Continuity State

### Settled

- one authoritative Timer/Ledger writer;
- SquareCoil remains company-clock authority;
- Bridge is observational/read-only;
- migration invocation and retained-legacy-key behavior;
- OWNER/OBSERVER native action 2/3/4 observation/forwarding behavior;
- positive READY relationship and verification fallback;
- Selected vs operational Context behavior;
- live tab Today + threshold semantics;
- native focus transition families and dirty-route deferral;
- Recent/overflow/hide/order behavior;
- Time Overview / History behavior;
- Archive/Clear Recent/Delete/Wipe distinction;
- Backup/Restore/CSV conflict and recovery behavior;
- Companion Light/Dark/Auto + Solid/Glass;
- SquareCoil Original/Refined Light/Sleek Dark;
- Timer Limits;
- Settings/draft/focus behavior;
- Support/Feedback/diagnostics privacy;
- presentation failure isolation.

### Provisional

Only non-core or implementation-policy details remain provisional, including:

- exact visual typography/spacing/animation;
- exact rendering strategy and drag implementation;
- exact storage/transport/checksum mechanics where Logic intentionally leaves implementation freedom;
- optional Cinematic Background detailed behavior;
- optional Design Dashboard presentation-profile detailed behavior.

### Open

None for the core behavior layer.

### Blocked

None at the core Logic layer.

Downstream canonical completion is still dependent on implementation and acceptance evidence. That dependency must not be mislabeled as missing Logic.

---

# 3. Optional Presentation Boundary

Historical Cinematic Background and Design Dashboard presentation are structurally preserved as optional post-core capabilities.

They do not block core implementation.

Detailed Logic for either should begin only after an upstream scope decision activates that optional capability for implementation.

Logic Systems Architect must not silently promote either optional pack into the core release requirement.

---

# 4. Reopen Rule

Do not reopen settled core Logic because implementation is incomplete.

Reopen only when one of these occurs:

```text
1. implementation exposes a genuine contradiction between settled rules;
2. new SquareCoil evidence invalidates an existing behavior assumption;
3. the user requests a new/changed behavior;
4. an optional presentation capability is formally activated and needs detailed behavior;
5. upstream framework structure changes in a way that invalidates the current behavior boundary.
```

Otherwise preserve settled decisions and continue downstream.

---

# 5. Current Readiness Judgment

**Core SquareCoil Companion behavior is implementation-ready.**

The next unresolved core work is downstream implementation and acceptance, beginning with the already-defined B2-C completion dependency and then canonical B3/B4/B5 slices.

Logic handoff is complete for the current core scope.
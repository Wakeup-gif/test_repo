[
  "Running `node index.js` prints exactly `Hello, World! The current time is HH:MM:SS.` to stdout where HH:MM:SS is a valid 24-hour time string.",
  "Running `node index.js Alice` prints exactly `Hello, Alice! The current time is HH:MM:SS.` to stdout.",
  "Process exits with code 0 on success.",
  "No external npm packages are installed or required.",
  "`node test/time.test.js` passes all assertions without error.",
  "package.json contains a `bin` field mapping `greet` to `./index.js`.",
  "package.json `engines` field specifies `node >=18`.",
  "index.js begins with the shebang line `#!/usr/bin/env node`.",
  "lib/time.js exports a single function `getFormattedTime` that returns a string matching `/^\\d{2}:\\d{2}:\\d{2}$/`.",
  "README.md documents usage, example output, and optional global install via `npm link`."
]